## Install

```
# 所有项目安装element-ui
# npx lerna add element-ui@latest

# 所有子项目添加axios
# npx lerna add axios --ignore main

# 某个子项目独立安装依赖
# yarn workspace main add core-js

yarn serve

# 或输出运行信息
# yarn serve --parallel
```

----